@model IEnumerable<BAL.ViewModels.Product.ProductDesignViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_ProductLayout.cshtml";
}

<h1>Product Designs</h1>

<p>
    <a asp-action="Create" asp-controller="ProductDesign" asp-area="Product"><customicon type="add" title="Create New Product Design"></customicon></a>
</p>
@if (!String.IsNullOrEmpty(ViewBag.Message))
{
    <div id="successMessage" style="padding-bottom:15px;" class="text-danger">
        <span class="@ViewBag.Class">@ViewBag.Message</span>
    </div>
}
@if (Model.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ProductDesignName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProductDesignName)
                    </td>
                    <td class="text-center">
                        <a href="@BAL.Common.WEBSITEPATH/product/productdesign/edit/@item.ProductDesignID"><customicon type="edit" title="Edit Product Design"></customicon></a> |
                        <a href="@BAL.Common.WEBSITEPATH/product/productdesign/details/@item.ProductDesignID"><customicon type="details" title="Show More Details"></customicon></a> |
                        <a href="@BAL.Common.WEBSITEPATH/product/productdesign/delete/@item.ProductDesignID" onclick="return confirm('Do you want to delete this record?')"><customicon type="delete" title="Delete Product Design"></customicon></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="text-center font-weight-bold m-5">
        <div class="form-control">No Records Found!</div>
        <div class="form-control">
            <a asp-action="Create">Create New Product Design</a>
        </div>
    </div>
}