@model IEnumerable<BAL.ViewModels.Product.ProductHeightViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_ProductLayout.cshtml";
}

    <h1>Product Heights</h1>

<p>
    <a asp-action="Create" asp-controller="ProductHeight" asp-area="Product"><customicon type="add" title="Create New Product Height"></customicon></a>
</p>
@if (!String.IsNullOrEmpty(ViewBag.Message))
{
    <div id="successMessage" style="padding-bottom:15px;" class="text-danger">
        <span class="@ViewBag.Class">@ViewBag.Message</span>
    </div>
}
@if (Model.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ProductHeightName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProductHeightName)
                    </td>
                    <td class="text-center">
                        <a href="@BAL.Common.WEBSITEPATH/product/productheight/edit/@item.ProductHeightID"><customicon type="edit" title="Edit Product Height"></customicon></a> |
                        <a href="@BAL.Common.WEBSITEPATH/product/productheight/details/@item.ProductHeightID"><customicon type="details" title="Show More Details"></customicon></a> |
                        <a href="@BAL.Common.WEBSITEPATH/product/productheight/delete/@item.ProductHeightID" onclick="return confirm('Do you want to delete this record?')"><customicon type="delete" title="Delete Product Height"></customicon></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="text-center font-weight-bold m-5">
        <div class="form-control">No Records Found!</div>
        <div class="form-control">
            <a asp-action="Create">Create New Product Height</a>
        </div>
    </div>
}

@section scripts {
    <script src="~/js/common.js" asp-append-version="true"></script>
}