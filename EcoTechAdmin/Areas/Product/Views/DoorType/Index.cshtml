@model IEnumerable<BAL.ViewModels.Product.DoorTypeViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_ProductLayout.cshtml";
}

<h1>Door Types</h1>

<p>
    <a asp-action="Create" asp-controller="DoorType" asp-area="Product"><customicon type="add" title="Create New Door Type"></customicon></a>
</p>
@if (!String.IsNullOrEmpty(ViewBag.Message))
{
    <div id="successMessage" style="padding-bottom:15px;" class="text-danger">
        <span class="@ViewBag.Class">@ViewBag.Message</span>
    </div>
}
@if (Model.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DoorTypeID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DoorTypeName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.DoorTypeID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DoorTypeName)
                    </td>
                    <td class="text-center">
                        <a href="@BAL.Common.WEBSITEPATH/product/doortype/edit/@item.DoorTypeID"><customicon type="edit" title="Edit Door Type"></customicon></a> |
                        <a href="@BAL.Common.WEBSITEPATH/product/doortype/details/@item.DoorTypeID"><customicon type="details" title="Show More Details"></customicon></a> |
                        <a href="@BAL.Common.WEBSITEPATH/product/doortype/delete/@item.DoorTypeID" onclick="return confirm('Do you want to delete this record?')"><customicon type="delete" title="Delete Door Type"></customicon></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="text-center font-weight-bold m-5">
        <div class="form-control">No Records Found!</div>
        <div class="form-control">
            <a asp-action="Create">Create New Door Type</a>
        </div>
    </div>
}