@model IEnumerable<BAL.ViewModels.Product.SubCategoryViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Sub Categories</h1>

<p>

    <a href="@BAL.Common.WEBSITEPATH/category/index/@TempData["SectionID"]"><img alt="Back to Categories" title="Back to Categories" src="~/img/back.png" border="0" style="border:none;" /></a>
    <a asp-action="Create" asp-route-id="@ViewBag.CategoryID"><img alt="Create New Sub Category" title="Create New Sub Category" src="~/img/create.png" border="0" style="border:none;" /></a>
    @if (ViewBag.CategoryID != null)
    {
        @Html.ActionLink("Show All Sub Categories", "Index", "SubCategory", new { id = "" })
    }
</p>
@if (!String.IsNullOrEmpty(ViewBag.Message))
{
    <div id="successMessage" style="padding-bottom:15px;" class="text-danger">
        <span class="@ViewBag.Class">@ViewBag.Message</span>
    </div>
}
@if (Model.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.SubCategoryName)
                </th>
                <th class="text-center">
                    @Html.DisplayNameFor(model => model.CategoryName)
                </th>
                <th class="text-center">Gallery</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.SubCategoryName)
                    </td>
                    <td class="text-center">
                        @Html.DisplayFor(modelItem => item.CategoryName)
                    </td>
                    <td class="text-center">
                        <a href="@BAL.Common.WEBSITEPATH/subcatgallery/index/@item.SubCategoryID/@item.CategoryID"><img alt="Manage Gallery" title="Manage Gallery" src="~/img/gallery.png" border="0" style="border:none;" /></a>
                    </td>
                    <td class="text-center">
                        <a href="@BAL.Common.WEBSITEPATH/subcategory/edit/@item.SubCategoryID"><img alt="Edit Sub Category" title="Edit Sub Category" src="~/img/edit.png" border="0" style="border:none;" /></a> |
                        <a href="@BAL.Common.WEBSITEPATH/subcategory/details/@item.SubCategoryID"><img alt="Show Full Details" title="Show Full Details" src="~/img/details.png" border="0" style="border:none;" /></a> |
                        <a href="@BAL.Common.WEBSITEPATH/subcategory/delete/@item.SubCategoryID" onclick="return confirm('Do you want to delete this record?')"><img alt="Delete Sub Category" title="Delete Sub Category" src="~/img/delete.jpeg" border="0" style="border:none;" /></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @section scripts {
        <script src="~/js/jquery-3.5.1.js" asp-append-version="true"></script>

        <script type="text/javascript">

            $(document).ready(function () {
                $('#successMessage').delay(5000).fadeOut('slow');
            });
        </script>
    }
}
else
{
    <div class="text-center font-weight-bold m-5">
        <div class="form-control">No Records Found!</div>
        <div class="form-control">
            <a asp-action="Create">Create New Sub Category</a>
        </div>
    </div>
}